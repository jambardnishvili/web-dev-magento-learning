{
  "name": "web-dev-magento-learning",
  "private": true,
  "scripts": {
    "docs:dev": ". .venv/bin/activate && mkdocs serve -a 127.0.0.1:8013",
    "docs:build": ". .venv/bin/activate && mkdocs build --strict",
    "docs:test": "playwright test",
    "docs:verify": ". .venv/bin/activate && mkdocs build --strict && playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.1"
  }
}
