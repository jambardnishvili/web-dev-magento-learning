# Case Studies

This section is reserved for concrete debugging walkthroughs that connect the conceptual pages to real failures.

Suggested future entries:

- request hits nginx but never reaches expected Magento route
- browser shows stale page because cache and response headers disagree
- admin and storefront behave differently because areas differ
- CLI behavior differs from web behavior because runtime context differs
- a cookie, session, or domain mismatch makes cart/login state look random

For now, use the first six pages to build the mental model these case studies will rely on.

